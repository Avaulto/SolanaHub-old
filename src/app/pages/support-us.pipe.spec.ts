import { SupportUsPipe } from './support-us.pipe';

describe('SupportUsPipe', () => {
  it('create an instance', () => {
    const pipe = new SupportUsPipe();
    expect(pipe).toBeTruthy();
  });
});
